// DEPRECATED — Phase 2 replaced this with components/layout/AppShell.tsx
// + components/layout/Sidebar.tsx. Nothing imports from this file
// anymore; it is safe to delete from disk. Left as an empty stub so
// any old import resolves to a no-op instead of a build error.
//
// The shared Icon helper that used to live here was moved to
// components/Icon.tsx — import from there if you need it.
export {};
