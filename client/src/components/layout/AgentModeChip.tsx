// DEPRECATED — Phase 2 polish replaced this chip with the Sub-agent
// dropdown inside the new ChatInputBar. Nothing imports from this
// file anymore; safe to delete from disk. Kept as a stub so any
// stale import resolves to a no-op instead of a build error.
export default function AgentModeChip() { return null; }
