services:
  tiger-cowork:
    build: .
    container_name: tiger-cowork
    ports:
      - "8080:3001"
    env_file:
      - .env
    volumes:
      - tiger-data:/app/data
      - tiger-uploads:/app/uploads
    restart: unless-stopped

volumes:
  tiger-data:
  tiger-uploads:
